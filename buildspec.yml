version: 0.2

phases:
  install:
    commands:
      - echo "📦 Installing dependencies"
      - pip install --upgrade pip
      - pip install -r requirements.txt
      - pip install xgboost

  build:
    commands:
      - echo "🚀 Running training script"
      - python train.py --bucket creditcarddata1204 --key data/train_data.csv

  post_build:
    commands:
      - echo "📦 Packaging model.pkl into model.tar.gz"
      - mkdir -p model_artifact
      - cp model/model.pkl model_artifact/
      - tar -czvf model.tar.gz -C model_artifact .

      - echo "📤 Uploading model.tar.gz to S3"
      - aws s3 cp model.tar.gz s3://creditcarddata1204/model-output-1306/model.tar.gz

      - echo "📤 Uploading metrics to S3"
      - aws s3 cp metrics/metrics.json s3://creditcarddata1204/model-output-1306/metrics.json

      - echo "✅ Upload complete"

artifacts:
  files:
    - model.tar.gz
